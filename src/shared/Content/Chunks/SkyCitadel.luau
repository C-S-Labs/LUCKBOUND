--!strict
-- Sky Citadel chunk kit. docs/biomes/SKY_CITADEL.md is the art direction; the pieces
-- are generated by assets/source/worlds/sky_citadel/build_sky_citadel_kit.py.
--
-- A white futurist citadel broken into floating islands above the cloud deck.
-- 22 pieces (2026-09-23), each built from a different mix of five axes --
-- island shape, floor, edge, keel, landmark -- so no two read alike.
--
-- DELIVERED AND UPLOADED 2026-09-23. Every piece measures exactly 256 cubed;
-- that was read off the delivered MeshParts rather than trusted, and each one
-- arrived with PivotOffset Y = -32, which is the walk plane sitting 32 studs
-- below the box centre -- GroundOffsetY = 96, confirmed by the art rather than
-- derived from the generator alone.
--
-- CONNECTION VOCABULARY -- decided before modelling, per CHUNK_AUTHORING.md
-- "Starting a different world". Deliberately disjoint from Verdant Valley's
-- PATH/WIDE; a test asserts the two sets never overlap.
--   SKYWAY  connective. A 40-stud bridge deck, walk plane at the origin.
--   ASCENT  arena-only. A 72-stud grand approach with glowing kerbs. Only
--           the two gate-courts offer one, so a gate-court precedes the arena
--           on every seed -- the same emergent rule as VV's Grove.
--
-- THE SHAPE OF A RUN. Four turning pieces (two each way), one crossroads, a
-- SIDE pocket, and three CAPs. ChunkCore picks a seeded exit among several, so
-- the crossroads can go straight, left or right; with IncludeSide on, the
-- lookout hangs off a spare socket. Nothing on the critical path dead-ends:
-- the vault that used to be the side pocket is now a turn you pass through.
-- Whatever socket is still open when all that is done gets a CAP (§7.4), so a
-- crossroads the path only half-uses no longer opens onto empty sky.
--
-- SUPPORTS is the scenario-compatibility gate (build spec §7.3): what can
-- HAPPEN on a piece, as opposed to what the piece IS. Derived per piece from
-- its own description rather than applied uniformly -- the Archive takes
-- puzzles and secrets, the Armory takes a mini-boss, the shattered span takes
-- traversal and an ambush and neither of the other two. A kit whose pieces all
-- accepted everything would make the metadata decoration.
--
-- Sockets: Facing is degrees, 0 = -Z (north), 90 = +X, 180 = +Z (south),
-- 270 = -X. Offsets are local to the piece, before rotation.
--
-- SIZE: 256 x 256 x 256 for EVERY piece, exact rather than nominal.
-- ChunkLoader sets mesh.Size = (SizeX, SizeY, SizeZ), so a mesh whose
-- bounding box differs is stretched, and a MeshPart pivots on its box centre.
-- The generator pins the box with four 0.3-stud edge pins at z = -96 and one
-- landmark per piece at +160. The walk plane therefore sits 32 studs BELOW
-- the box centre, which GroundOffsetY = 96 applies at the bottom of this file.

local function socket(id: string, kind: string, x: number, z: number, facing: number)
	return { Id = id, Kind = kind, OffsetX = x, OffsetY = 0, OffsetZ = z, Facing = facing }
end

local CHUNKS = {
	{
		Id = "SC_ENTRY",
		Role = "ENTRY" :: any,
		AssetKey = "SC_CHUNK_ENTRY",
		-- Octagonal arrival plaza under the Beacon. The spawn drops onto the landing pad, so
		-- nothing is built above (0, 0); the south quarter is kept clear for the return portal.
		Sockets = { socket("north", "SKYWAY", 0, -128, 0) },
		Weight = 1,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "arrival", "low-density" },
	},
	{
		Id = "SC_PATH_STRAIGHT",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_PATH_STRAIGHT",
		-- The gatehouse pier: a skyway under an arch between two turrets.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Combat = true, Traversal = true, Ambush = true },
		Weight = 30,
		EnemyTags = {},
		Tags = { "bridge", "narrow" },
	},
	{
		Id = "SC_PATH_SKYPORT",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_PATH_SKYPORT",
		-- A plank pier with a dock islet off it: hazard pad, crane, a moored skiff.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Combat = true, Traversal = true, Treasure = true },
		Weight = 20,
		EnemyTags = {},
		Tags = { "bridge", "dock" },
	},
	{
		Id = "SC_PATH_HOOPS",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_PATH_HOOPS",
		-- A bare glass-floored span through three floating rings, between two satellites.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Traversal = true, Combat = true, Ambush = true },
		Weight = 18,
		EnemyTags = {},
		Tags = { "bridge", "narrow", "exposed" },
	},
	{
		Id = "SC_PATH_SHATTERED",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_PATH_SHATTERED",
		-- The span is broken: eight floating plates, a stride apart, and the Sundered Spire
		-- whose crown hangs over its stump.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Traversal = true, Ambush = true, Secret = true },
		Weight = 18,
		EnemyTags = {},
		Tags = { "bridge", "broken", "exposed" },
	},
	{
		Id = "SC_PATH_AVIARY",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_PATH_AVIARY",
		-- The path runs through a gilded birdcage dome, birds in and around it.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Combat = true, Ambush = true, Event = true },
		Weight = 16,
		EnemyTags = {},
		Tags = { "bridge", "garden" },
	},
	{
		Id = "SC_CROSSROADS",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_CROSSROADS",
		-- THE INTERSECTION. Four SKYWAY mouths under the Arcane Prism. The path leaves by a
		-- seeded choice of the other three; a SIDE pocket may take a spare one.
		Sockets = {
			socket("south", "SKYWAY", 0, 128, 180),
			socket("north", "SKYWAY", 0, -128, 0),
			socket("east", "SKYWAY", 128, 0, 90),
			socket("west", "SKYWAY", -128, 0, 270),
		},
		Supports = { Combat = true, Ambush = true, Shrine = true, Event = true },
		Weight = 14,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "intersection" },
	},
	{
		Id = "SC_PATH_BEND",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_PATH_BEND",
		-- Turns right round the Spired Keep.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("east", "SKYWAY", 128, 0, 90) },
		Supports = { Combat = true, Traversal = true, Ambush = true },
		Weight = 22,
		EnemyTags = {},
		Tags = { "turn", "right" },
	},
	{
		Id = "SC_PATH_BEND_WEST",
		Role = "PATH" :: any,
		AssetKey = "SC_CHUNK_PATH_BEND_WEST",
		-- Turns left on a dark engine-deck, the lighthouse on its own islet.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("west", "SKYWAY", -128, 0, 270) },
		Supports = { Combat = true, Traversal = true, Ambush = true },
		Weight = 22,
		EnemyTags = {},
		Tags = { "turn", "left" },
	},
	{
		Id = "SC_VAULT_TURN",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_VAULT_TURN",
		-- The treasury, passed THROUGH on a right turn -- no longer a dead end.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("east", "SKYWAY", 128, 0, 90) },
		Supports = { Puzzle = true, Treasure = true, Secret = true, Combat = true, MiniBoss = true },
		Weight = 14,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "turn", "right", "loot" },
	},
	{
		Id = "SC_AETHER_SPRINGS",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_AETHER_SPRINGS",
		-- Terraced pools and the Cascade Tower, water falling off the east rim; turns left.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("west", "SKYWAY", -128, 0, 270) },
		Supports = { Shrine = true, Event = true, Combat = true, Treasure = true },
		Weight = 14,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "turn", "left", "water" },
	},
	{
		Id = "SC_GARDEN_TERRACE",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_GARDEN_TERRACE",
		-- The hanging gardens: three islets, the Sky Tree, a pool, a gazebo.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Combat = true, Shrine = true, Ambush = true, Treasure = true },
		Weight = 16,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "garden", "islands" },
	},
	{
		Id = "SC_OBSERVATORY",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_OBSERVATORY",
		-- A night-dark deck of stars: dome and mast, an orrery, dishes on a raised deck.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Puzzle = true, Secret = true, Event = true, Combat = true },
		Weight = 14,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "landmark", "night" },
	},
	{
		Id = "SC_ARMORY",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_ARMORY",
		-- A slate training yard and a barracks islet, under the banner mast.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Combat = true, MiniBoss = true, Treasure = true, Ambush = true },
		Weight = 14,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "yard", "islands" },
	},
	{
		Id = "SC_ARCHIVE",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_ARCHIVE",
		-- A roofed hall of shelves and ribbed vaults, floating tomes, the clock tower.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "SKYWAY", 0, -128, 0) },
		Supports = { Puzzle = true, Secret = true, Treasure = true, Combat = true },
		Weight = 14,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "hall", "roofed" },
	},
	{
		Id = "SC_SPIRE_COURT",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_SPIRE_COURT",
		-- Gate-court: the Sky Fountain and the Ascent Gate. An ASCENT provider.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "ASCENT", 0, -128, 0) },
		Supports = { Combat = true, MiniBoss = true, Event = true, Shrine = true },
		Weight = 25,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "open", "gate-to-boss" },
	},
	{
		Id = "SC_SPIRE_COURT_B",
		Role = "COMBAT" :: any,
		AssetKey = "SC_CHUNK_SPIRE_COURT_B",
		-- Gate-court, rare: the Hall of Winds and its Moon Gate. The other ASCENT provider.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180), socket("north", "ASCENT", 0, -128, 0) },
		Supports = { Combat = true, MiniBoss = true, Event = true, Traversal = true },
		Weight = 8,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "open", "gate-to-boss", "rare" },
	},
	{
		Id = "SC_SIDE_LOOKOUT",
		Role = "SIDE" :: any,
		AssetKey = "SC_CHUNK_SIDE_LOOKOUT",
		-- The world's SIDE pocket: a lookout and a loot cache, hung off a spare socket (a
		-- crossroads branch, typically) when IncludeSide is on.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180) },
		Supports = { Treasure = true, Secret = true, Shrine = true },
		Weight = 100,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "optional", "risk-reward" },
	},
	-- THE CAPS (build spec §7.4). Three different reasons a skyway stops, so a
	-- map with several loose ends does not end the same way three times. Each
	-- takes a SKYWAY on its south edge and offers nothing further: a cap is
	-- where the citadel runs out, and there is no walking past one.
	--
	-- No MaxPerLayout, deliberately, and Schema refuses one: a layout needs as
	-- many caps as it happens to have open sockets, and an allowance that ran
	-- out would leave exactly the raw openings caps exist to close.
	{
		Id = "SC_CAP_OVERLOOK",
		Role = "CAP" :: any,
		AssetKey = "SC_CHUNK_CAP_OVERLOOK",
		-- The skyway arrives at a railed overlook over the cloud deck: the
		-- kindest of the three endings, and somewhere worth the walk.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180) },
		Weight = 40,
		EnemyTags = {},
		Tags = { "cap", "vista" },
	},
	{
		Id = "SC_CAP_CRUMBLING",
		Role = "CAP" :: any,
		AssetKey = "SC_CHUNK_CAP_CRUMBLING",
		-- The span breaks off mid-air. The path stops because it fell, which
		-- is the reading that costs the player nothing to understand.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180) },
		Weight = 35,
		EnemyTags = {},
		Tags = { "cap", "broken" },
	},
	{
		Id = "SC_CAP_SEALED_GATE",
		Role = "CAP" :: any,
		AssetKey = "SC_CHUNK_CAP_SEALED_GATE",
		-- A gate that stays shut: the one ending that implies somebody closed
		-- it, and the only one that would read as a lie if it ever opened.
		Sockets = { socket("south", "SKYWAY", 0, 128, 180) },
		Weight = 25,
		EnemyTags = {},
		Tags = { "cap", "sealed" },
	},
	{
		Id = "SC_BOSS_CLEARING",
		Role = "BOSS" :: any,
		AssetKey = "SC_CHUNK_BOSS_CLEARING",
		-- The Crown Spire arena. Accepts only ASCENT.
		Sockets = { socket("south", "ASCENT", 0, 128, 180) },
		Weight = 1,
		MaxPerLayout = 1,
		EnemyTags = {},
		Tags = { "arena" },
	},
}

local out = {}
for _, chunk in CHUNKS do
	local c = table.clone(chunk) :: any
	c.WorldId = "SKY_CITADEL"
	c.SizeX = 256
	c.SizeY = 256
	c.SizeZ = 256
	-- The generator pins every box to 256 cubed with the walk plane 32 studs
	-- BELOW the box centre, so the ground sits 96 studs up from the bottom.
	-- ChunkLoader lifts the mesh by (SizeY/2 - GroundOffsetY); without this
	-- every Sky Citadel piece would sit 32 studs into the floor. The header
	-- above derived this number and nothing had yet applied it.
	c.GroundOffsetY = 96
	table.insert(out, c)
end
return out
