name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # before Install Luau: that step unpacks binaries into the checkout
      - name: Repository index is current (INDEX_MAP.md)
        run: python3 tools/gen_index.py --check

      - name: Install Luau
        run: |
          curl -sSL -o luau.zip https://github.com/luau-lang/luau/releases/latest/download/luau-ubuntu.zip
          unzip -q luau.zip
          chmod +x luau luau-analyze
          echo "$PWD" >> "$GITHUB_PATH"

      - name: Syntax check
        run: |
          fail=0
          while IFS= read -r f; do
            if luau-analyze "$f" 2>&1 | grep -q "SyntaxError"; then
              echo "::error file=$f::syntax error"
              luau-analyze "$f" 2>&1 | grep "SyntaxError"
              fail=1
            fi
          done < <(find src tests -name '*.luau' ! -name 'generated_suite.luau')
          exit $fail

      - name: Forbidden module names (build spec P1-12)
        run: |
          hits=$(find src -name '*_Final*' -o -name '*_NEW*' -o -name '*_FIXED*' \
                          -o -name '*_v2*' -o -name '*_old*' -o -name '*_backup*')
          if [ -n "$hits" ]; then
            echo "::error::duplicate-module names found -- see AGENTS.md rule 1"
            echo "$hits"
            exit 1
          fi

      - name: Unit tests
        run: |
          python3 tests/build_suite.py
          luau tests/generated_suite.luau

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Strict since 2026-09-25 (whole codebase formatted once). Version pinned to rokit.toml.
      # Generated content files are skipped via .styluaignore.
      - name: StyLua
        uses: JohnnyMorganz/stylua-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: 2.0.2
          args: --check src tests

  handoff:
    # A PR that changes src/ or assets/ must also update docs/STATUS.md or docs/WORKLOG.md, so the handoff never goes
    # stale. Bypass for trivial PRs: put [skip-status] in the PR title.
    if: github.event_name == 'pull_request' && !contains(github.event.pull_request.title, '[skip-status]')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Handoff docs updated with the change
        run: |
          changed=$(git diff --name-only origin/${{ github.base_ref }}...HEAD)
          if echo "$changed" | grep -qE '^(src|assets)/' && ! echo "$changed" | grep -qE '^docs/(STATUS|WORKLOG)\.md$'; then
            echo "::error::This PR changes src/ or assets/ but not docs/STATUS.md or docs/WORKLOG.md (AGENTS.md: Before you finish). Add [skip-status] to the title if trivial."
            exit 1
          fi
