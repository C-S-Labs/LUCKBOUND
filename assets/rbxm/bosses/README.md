# Boss models for /showboss

Save each imported boss Model here as `<Name>.rbxmx` (the name used in `src/shared/Content/BossPreviews.luau`).
Rojo syncs this folder to `ServerStorage.LuckboundBosses`.
